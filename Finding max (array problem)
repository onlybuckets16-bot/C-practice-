#include <stdio.h>

int main()
{
    int nums[7] = {12, 5, 27, 8, 19, 3, 15};
    int max = nums[0];

    for (int i = 1; i < 7; i++) {

        if (nums[i] > max) {
            max = nums[i];
        }
    }

    printf("%d", max);

    return 0;
}
